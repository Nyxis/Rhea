new_workflow:
    pattern: '/create'
    defaults:
        _controller: EasyTaskWorkflowBundle:Workflow:form
        _format: html

edit_workflow:
    pattern: '/edit/{workflow_id}'
    defaults:
        _controller: EasyTaskWorkflowBundle:Workflow:form
        _format: html
    requirements:
        workflow_id: \d+
