_management:
    resource: "routing/management.yml"
    prefix: /user

_consultant:
    resource: "routing/consultant.yml"
    prefix: /team/consultant
    defaults:
        _menu: users
        _submenu: consultant

_internal:
    resource: "routing/internal.yml"
    prefix:   /internal

# ---------------------------------------
# Authentication
# ---------------------------------------
_authentication:
    resource: "routing/login.yml"
    prefix: /
