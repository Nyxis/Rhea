{{ 'annual_review_preparing.notification'|trans({
        '%mgr%': task.userAssigned.longName,
        '%clt%': task.userTarget.longName,
        '%notif_date%': task.data.get('notif_date')|date('md'|trans)
    })
}}
