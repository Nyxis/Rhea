{{
    'mission_monitoring.bootstrap.notification'|trans({
        '%clt%': task.target('consultant').longName,
        '%client%': task.target('consultant').consultant.currentMission.client.title,
        '%notif_date%': task.data.get('notif_date')|date('d/m')
    })
}}
